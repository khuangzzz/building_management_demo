import '../proto/auth.pbserver.dart';
import 'dart:async';
import 'package:http/http.dart';

class LoginService {
  LoginResponse login(String email, String password) {
    return LoginResponse();
  }
}

